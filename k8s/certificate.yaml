apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: mkjs-cert

spec:
  secretName: mkjs-tls

  dnsNames:
    - mkjs.local

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer